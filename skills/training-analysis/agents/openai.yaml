interface:
  display_name: "Training Analysis"
  short_description: "Sync provider data and update goal-aware training notes"
  default_prompt: "Use $training-analysis to verify the receiver-backed Apple Health cache, sync Strava, analyze a run against the plan and dedicated goal sources, update workout notes or changelog entries, rebuild generated data, and commit the result."
