[project]
name = "measured-local-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "strava-mcp",
]

[tool.uv]
package = false

[tool.uv.sources]
strava-mcp = { path = "tools/strava-mcp", editable = true }
